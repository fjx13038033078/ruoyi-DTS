<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ticket.mapper.BizOrderMapper">

    <resultMap type="com.ruoyi.ticket.domain.BizOrder" id="BizOrderResult">
        <id     property="orderId"        column="order_id"        />
        <result property="orderNo"        column="order_no"        />
        <result property="userId"         column="user_id"         />
        <result property="sessionId"      column="session_id"      />
        <result property="totalAmount"     column="total_amount"    />
        <result property="status"         column="status"          />
        <result property="transactionId"   column="transaction_id"  />
        <result property="createTime"      column="create_time"     />
        <result property="payTime"         column="pay_time"       />
    </resultMap>

    <sql id="selectBizOrderVo">
        select order_id, order_no, user_id, session_id, total_amount, status, transaction_id, create_time, pay_time
        from biz_order
    </sql>

    <select id="selectBizOrderById" parameterType="Long" resultMap="BizOrderResult">
        <include refid="selectBizOrderVo"/>
        where order_id = #{orderId}
    </select>

    <select id="selectBizOrderList" parameterType="com.ruoyi.ticket.domain.BizOrder" resultMap="BizOrderResult">
        <include refid="selectBizOrderVo"/>
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND order_no = #{orderNo}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="sessionId != null">
                AND session_id = #{sessionId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                AND date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                AND date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
    </select>

    <insert id="insertBizOrder" parameterType="com.ruoyi.ticket.domain.BizOrder" useGeneratedKeys="true" keyProperty="orderId">
        insert into biz_order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderNo != null and orderNo != ''">order_no,</if>
            <if test="userId != null">user_id,</if>
            <if test="sessionId != null">session_id,</if>
            <if test="totalAmount != null">total_amount,</if>
            <if test="status != null">status,</if>
            <if test="transactionId != null">transaction_id,</if>
            <if test="payTime != null">pay_time,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderNo != null and orderNo != ''">#{orderNo},</if>
            <if test="userId != null">#{userId},</if>
            <if test="sessionId != null">#{sessionId},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            <if test="status != null">#{status},</if>
            <if test="transactionId != null">#{transactionId},</if>
            <if test="payTime != null">#{payTime},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateBizOrder" parameterType="com.ruoyi.ticket.domain.BizOrder">
        update biz_order
        <set>
            <if test="orderNo != null and orderNo != ''">order_no = #{orderNo},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="sessionId != null">session_id = #{sessionId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="transactionId != null">transaction_id = #{transactionId},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
        </set>
        where order_id = #{orderId}
    </update>

    <delete id="deleteBizOrderById" parameterType="Long">
        delete from biz_order where order_id = #{orderId}
    </delete>

    <delete id="deleteBizOrderByIds" parameterType="Long">
        delete from biz_order where order_id in
        <foreach item="orderId" collection="array" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </delete>

    <update id="updateStatusToCancelledByIds">
        update biz_order set status = '2'
        where order_id in
        <foreach item="orderId" collection="orderIds" open="(" separator="," close=")">
            #{orderId}
        </foreach>
        and status = '0'
    </update>

</mapper>
